@Library('my-shared-library') _

def deployParams = [
    deployName: 'java-maven-app',
    repoName: 'ahmedgamalhamdy/java-maven-app:latest',
    stage: 'dev' // Change this to 'test' or 'release' as needed
]

deployPipeline(deployParams)
